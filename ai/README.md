# ZYLO AI assets

Place your existing file here:

`interior_weights_finetuned.weights.h5`

The file is intentionally excluded from Git because model weights are large and may have licensing/storage considerations.
